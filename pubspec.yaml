name: notes_app

description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  dartz: ^0.10.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_test:
    sdk: flutter
    version: ^0.0.0
  freezed_annotation: ^1.1.0

dev_dependencies:
  build_runner: null
  freezed: ^1.1.1

flutter:

  uses-material-design: true
